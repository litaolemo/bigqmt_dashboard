{
  "_note": "股票基础库（bak_basic / etf_type_snapshot），只读账号即可。复制为 config/mysql.json 后填写；也可改用 STOCK_DB_HOST/PORT/USER/PASSWORD/NAME 环境变量（环境变量优先）。不配置则下单搜索只走本地缓存。",
  "host": "your-mysql-host",
  "port": 3306,
  "user": "readonly_user",
  "password": "",
  "database": "stock_db_daily",
  "charset": "utf8"
}
