# Changelog

面向用户可见的变更。日期是推送到这个仓库的日期，不是语义化版本号——项目还没有正式发版节奏，条目按推送批次分组，新的在上面。

## [未发布]

### 新增

- **条件单界面**：持仓行加一列闹钟图标（已挂条件单时带角标数量），点开的弹窗按「想干什么」分四页覆盖全部 6 种触发类型——保护持仓（止损+止盈，可以一起设，出来是两张各自独立的单）、涨停破板卖、条件买入（下探/突破）、涨停买入。触发价可填绝对价格或涨跌幅百分比；止损带「保本」快捷键一键填成本价。提交前把「将创建几张、每张什么条件」摊开显示。买卖流水下面新增条件单列表，可撤销。
- **条件单**：止损、止盈、条件买入（下探/突破）、涨停破板卖出、涨停买入，6 种触发类型。后台每 5 秒查一次价，越过触发线就走和手动下单同一条链路真的报单——风控闸门、价格笼子、审计流水一个都不少。触发价可以给绝对价格，也可以给涨跌幅百分比（创建时按当时昨收换算）；涨停破板/涨停买入两种的触发价是当天动态算出的涨停价，不用填。下单前有原子占位，避免落库失败导致同一条件被重复触发。API：`POST /api/conditional-orders`、`GET /api/conditional-orders`、`POST /api/conditional-orders/cancel`。界面见上一条。
- **实时推送**：`GET /ws/updates`，WebSocket 把委托状态变化、真实成交、条件单触发秒级推给浏览器，取代原来几秒一次的轮询。
- **通知**：成交、条件单触发/失败可以推送到企业微信群机器人和/或 Server 酱，配置 `WECOM_WEBHOOK_URL` / `SERVERCHAN_KEY` 或 `config/notify.json`，缺配置只打日志。
- **全局熔断**：一天内买卖撤单合计次数超过阈值（`DAILY_ACTION_LIMIT`，默认 2000）自动拒绝所有后续下单/撤单，防的是软件自己失控（脚本死循环、条件单反复误触发之类）。买卖/撤单/条件单触发/国债逆回购共用同一个计数。`GET /api/accounts/health` 能看到当前计数，`POST /api/admin/reset-daily-action-count` 手动复位。
- **国债逆回购自动出借**：每个交易日 15:00-15:05，给开了开关的账号把闲置现金出借到深市 131810 / 沪市 204001 中利率更高的那个。按账号开关，默认关闭。`POST /api/account/reverse-repo` 开/关。

- **条件单也记住报价方式/交易类型**：和手动下单的记忆分成两套（各自一张表），按（人、账号、方向）存，建单成功才写。留空时沿用上次建条件单用的那个，用不了就退回默认（`peer` 对手方最优）而不是把建单卡掉。两套刻意不互通——手动习惯挂「最新价」继承给止损单，就是一张报不掉的止损单。条件单弹窗取默认值用 `GET /api/instrument/{code}?scope=conditional`。

- **出入金入口**：「累计盈亏」卡片上加了「出入金」标记和悬停说明。这张卡一直是资金调整弹窗的唯一入口，但卡片上没有按钮也没有图标，只有鼠标会变手型，没人找得到（[#2](https://github.com/litaolemo/bigqmt_dashboard/issues/2)）。README 也补了一节说明公式和自动补记的门槛。

### 变更

- 自动记出入金的取样窗口收窄成 **09:10-09:15** 和 **15:00-15:05**（原来是 08:00-09:15 和 15:00-16:00）。只有这两段总资产是静的，前后两次快照的差额除了出入金没有别的来源；窗口开宽了会把市值波动当成转账记进去，盈亏就永久错了。判定提成了 `in_capital_sample_window()`，边界补了测试——这段逻辑原来是埋在 `save_asset()` 里的裸表达式，一直没有测试覆盖。

### 修复

- 条件单列表的创建/触发时间显示差 8 小时：这张表用 `CURRENT_TIMESTAMP`，存的是 UTC。存储不动（触发引擎的重试通知冷却是拿 `utcnow()` 跟它比的），只在送给界面时换成本地时间。
- 汇总视图下「记住上次的报价方式/交易类型」不生效，信用账户的融资融券选项也会从下单弹窗里消失。原因是汇总持仓行的 `account_id` 是后端拼的大写 `ALL`，而前端只把小写 `all` 当作哨兵值，于是把 `account_id=ALL` 当成真账号发给了服务端——查不到就静默退回普通账户。管理员登录后默认停在汇总视图，等于这两个功能在默认视图下一直没工作过，而且界面上没有任何报错。前端改成大小写无关，服务端也一并把 `all`/`ALL` 认成「不指定账号」。

## 2026-09-03

### 修复

- 分笔成交不再互相覆盖（[#1](https://github.com/litaolemo/bigqmt_dashboard/issues/1)）：`trades` 表的唯一约束从 `(order_id, order_sysid)` 改成 `(account_id, traded_id)`——同一笔委托的多笔分笔成交共享前两个字段，旧约束会把它们当同一行反复覆盖，只留下最后一笔。旧库自动迁移，历史数据原样保留，但已经被覆盖掉的分笔成交救不回来。

## 2026-09-01

### 新增

- 报价方式全表（21 种选价类型，按交易所过滤：沪/北专有的 42/43、深专有的 46/47/48 不会出现在对方交易所的标的上）。
- 价格笼子：连续竞价基准价 ±2%，集合竞价基准价 ±10%，与涨跌停取交集，超出当场拒单并说明上下限。
- 买卖指令类型：区分普通买卖（23/24）与信用账户的融资融券（27/28/29/31），按账户类型给出可选项和默认值。
- 未成交委托视图 + 撤单按钮。
- 报价方式与交易类型的选择会记住，按（人、账号、方向）存，下次开弹窗直接预选。

### 修复

- 担保品买卖（33/34）暂不支持——它在 xtconstant 里没有独立值，直接给会退化成普通买入，先撤掉这个选项，信用账户默认走普通买卖。

## 2026-08-30

首次发布。

### 新增

- 大QMT 直连的多账号持仓监控与下单面板：实时持仓、资金曲线、交易日历、盈亏矫正。
- 下单：按比例卖、按数量卖、按数量买、按金额买（服务端用实时价换算）、一键清仓，全部当场报单并返回真实委托号。
- 服务端风控：停买/停卖/锁仓/仓位系数/展示模式，判定不过就不会有报单。
- 可转债：10 张步进、0.001 报价精度、溢价率/转股价值/双低、强赎进度与禁买闸门、打新债。
- 买卖流水：账户真实成交，买卖同表，取代原来展示选股信号的「历史买入列表」。
- 观察者账号体系：独立的只读账号，只看得到买卖流水。
- 研究记录板：粘贴自然语言选股逻辑，大模型解析成结构化记录 + K线研判（可选）。
- 多账号多实例，每账号独立连接。

### 修复

- 强赎进度区分「无数据」与「0 次命中」，数据不足时不再误判为未触发。
- `/api/users/me` 因为列名错位泄露明文清仓密码。
